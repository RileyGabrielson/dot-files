require("lsp_signature").setup({
    bind = true,
    fix_pos = true,
    hi_parameter = 'IncSearch',
    always_trigger = false,
    hint_enable = true,
    floating_window = false,
    hint_prefix = "▷ ",
})
